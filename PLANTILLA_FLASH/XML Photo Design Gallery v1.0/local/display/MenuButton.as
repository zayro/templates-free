package local.display{	import flash.display.Sprite;	import flash.events.MouseEvent;	import flash.text.TextField;	import flash.text.TextFieldAutoSize;		import elemental.effects.ColorFade;		public class MenuButton extends Sprite	{		private var closure: Function;		private var args: Array;				public function MenuButton(text: String = null, closure: Function = null, args: Array = null) : void		{			if (text == null)				return void;							_txt.text = text;			this.closure = closure;			this.args = args;						addEventListener(MouseEvent.MOUSE_OVER, onMouseOver);			addEventListener(MouseEvent.MOUSE_OUT,  onMouseOut);			addEventListener(MouseEvent.CLICK,      onMouseClick);						this.buttonMode = true;			this.useHandCursor = true;			this.mouseChildren = false;						this._txt.autoSize = TextFieldAutoSize.LEFT;		}				private function onMouseOver(event: MouseEvent) : void		{			ColorFade.fadeTo(this, 0, 0,0,0, 4);		}				private function onMouseOut(event: MouseEvent) : void		{			ColorFade.fadeTo(this, 1, 0,0,0, 4);		}				private function onMouseClick(event: MouseEvent) : void		{			if (args)				closure(args);			else				closure();		}	}}