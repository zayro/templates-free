package com.massiveProCreation.ball.utils{	import flash.display.*;	import flash.events.*;	import caurina.transitions.*;	import flash.text.TextField;		public class ScrollBox extends MovieClip	{				public function ScrollBox():void		{			/// this class repersents the box for scroll bar which is being scrolled			sb.addEventListener(ScrollBarEvent.VALUE_CHANGED, sbChange);		}				private function sbChange(e:ScrollBarEvent):void		{			Tweener.addTween(content, {y:(-e.scrollPercent*((content.height + 10)-masker.height)),							   time:1});		}	}}